<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"file:///C:\Users\805-06\Desktop\TP\5\Java\Trekkit_Java\src\main\resources\dtd\mybatis-3-mapper.dtd">
<mapper namespace="com.Trekkit_Java.DAO.QnaDAO">

    <!-- Q&A 질문 관련 쿼리 -->
    <select id="getAllQuestions" resultType="com.Trekkit_Java.DTO.QnaQuestionDTO">
        SELECT
            id, user_id as userId, nickname, title, content, mountain,
            view_count as viewCount, answer_count as answerCount, like_count as likeCount,
            is_solved as isSolved, accepted_answer_id as acceptedAnswerId,
            created_at as createdAt, updated_at as updatedAt
        FROM qna_questions
        ORDER BY created_at DESC
    </select>

    <select id="getQuestionById" resultType="com.Trekkit_Java.DTO.QnaQuestionDTO">
        SELECT
            id, user_id as userId, nickname, title, content, mountain,
            view_count as viewCount, answer_count as answerCount, like_count as likeCount,
            is_solved as isSolved, accepted_answer_id as acceptedAnswerId,
            created_at as createdAt, updated_at as updatedAt
        FROM qna_questions
        WHERE id = #{id}
    </select>

    <insert id="insertQuestion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO qna_questions (user_id, nickname, title, content, mountain, created_at, updated_at)
        VALUES (#{userId}, #{nickname}, #{title}, #{content}, #{mountain}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateQuestion">
        UPDATE qna_questions
        SET
            title = #{title},
            content = #{content},
            mountain = #{mountain},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteQuestion">
        DELETE FROM qna_questions
        WHERE id = #{id}
    </delete>

    <update id="incrementQuestionViewCount">
        UPDATE qna_questions
        SET view_count = view_count + 1
        WHERE id = #{id}
    </update>

    <update id="incrementQuestionAnswerCount">
        UPDATE qna_questions
        SET answer_count = answer_count + 1
        WHERE id = #{id}
    </update>

    <update id="decrementQuestionAnswerCount">
        UPDATE qna_questions
        SET answer_count = answer_count - 1
        WHERE id = #{id}
    </update>

    <update id="updateQuestionSolvedStatus">
        UPDATE qna_questions
        SET
            is_solved = #{isSolved},
            accepted_answer_id = #{acceptedAnswerId}
        WHERE id = #{questionId}
    </update>

    <!-- Q&A 답변 관련 쿼리 -->
    <select id="getAnswersByQuestionId" resultType="com.Trekkit_Java.DTO.QnaAnswerDTO">
        SELECT
            id, question_id as questionId, user_id as userId, nickname, content,
            like_count as likeCount, is_accepted as isAccepted,
            created_at as createdAt, updated_at as updatedAt
        FROM qna_answers
        WHERE question_id = #{questionId}
        ORDER BY created_at ASC
    </select>

    <select id="getAnswerById" resultType="com.Trekkit_Java.DTO.QnaAnswerDTO">
        SELECT
            id, question_id as questionId, user_id as userId, nickname, content,
            like_count as likeCount, is_accepted as isAccepted,
            created_at as createdAt, updated_at as updatedAt
        FROM qna_answers
        WHERE id = #{id}
    </select>

    <insert id="insertAnswer" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO qna_answers (question_id, user_id, nickname, content, created_at, updated_at)
        VALUES (#{questionId}, #{userId}, #{nickname}, #{content}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateAnswer">
        UPDATE qna_answers
        SET
            content = #{content},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteAnswer">
        DELETE FROM qna_answers
        WHERE id = #{id}
    </delete>

    <update id="updateAnswerAcceptedStatus">
        UPDATE qna_answers
        SET is_accepted = #{isAccepted}
        WHERE id = #{answerId}
    </update>

    <!-- Q&A 이미지 관련 쿼리 (모두 제거) -->


    <!-- Q&A 질문 좋아요 관련 쿼리 -->
    <insert id="insertQuestionLike">
        INSERT INTO qna_question_likes (question_id, user_id, created_at)
        VALUES (#{questionId}, #{userId}, #{createdAt})
    </insert>

    <delete id="deleteQuestionLike">
        DELETE FROM qna_question_likes
        WHERE question_id = #{questionId} AND user_id = #{userId}
    </delete>

    <select id="getQuestionLikeCount" resultType="int">
        SELECT COUNT(*)
        FROM qna_question_likes
        WHERE question_id = #{questionId}
    </select>

    <select id="checkQuestionLikeStatus" resultType="com.Trekkit_Java.DTO.QnaLikeDTO">
        SELECT id, question_id as questionId, user_id as userId, created_at as createdAt
        FROM qna_question_likes
        WHERE question_id = #{questionId} AND user_id = #{userId}
    </select>

    <update id="incrementQuestionLikeCount">
        UPDATE qna_questions
        SET like_count = like_count + 1
        WHERE id = #{questionId}
    </update>

    <update id="decrementQuestionLikeCount">
        UPDATE qna_questions
        SET like_count = like_count - 1
        WHERE id = #{questionId}
    </update>

    <!-- Q&A 답변 좋아요 관련 쿼리 -->
    <insert id="insertAnswerLike">
        INSERT INTO qna_answer_likes (answer_id, user_id, created_at)
        VALUES (#{answerId}, #{userId}, #{createdAt})
    </insert>

    <delete id="deleteAnswerLike">
        DELETE FROM qna_answer_likes
        WHERE answer_id = #{answerId} AND user_id = #{userId}
    </delete>

    <select id="getAnswerLikeCount" resultType="int">
        SELECT COUNT(*)
        FROM qna_answer_likes
        WHERE answer_id = #{answerId}
    </select>

    <select id="checkAnswerLikeStatus" resultType="com.Trekkit_Java.DTO.QnaLikeDTO">
        SELECT id, answer_id as answerId, user_id as userId, created_at as createdAt
        FROM qna_answer_likes
        WHERE answer_id = #{answerId} AND user_id = #{userId}
    </select>

    <update id="incrementAnswerLikeCount">
        UPDATE qna_answers
        SET like_count = like_count + 1
        WHERE id = #{answerId}
    </update>

    <update id="decrementAnswerLikeCount">
        UPDATE qna_answers
        SET like_count = like_count - 1
        WHERE id = #{answerId}
    </update>

</mapper>
